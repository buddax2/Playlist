#set("title") { Hello, #(name)! }

#set("body") {
    <ul>
    #for(song in songs) {
        <li><a target=\"_blank\" href=#(song.youtubeURL)><img src="/images/favicon-youtube.ico"></a> | <a target=\"_blank\" href=#(song.musicURL)><img width"16px" height="16px" src="/images/favicon-music.ico"></a> #(song.title)</li>
    }
    </ul>
}

#embed("base")
